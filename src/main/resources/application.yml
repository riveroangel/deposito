spring:
  application:
    name: gamasonic

  datasource:
    url: jdbc:postgresql://localhost:5432/depositodb
    driver-class-name: org.postgresql.Driver
    username: miguel1
    password: _cLg9qnviY-"u%,

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    h2:
      console:
        enabled: false

  # ⭐ CORREGIDO: Archivos estáticos desde el classpath
  web:
    resources:
      static-locations: classpath:/static/  # ← CAMBIADO
      add-mappings: true
      cache:
        period: 0

  # ⭐ AGREGADO: Configuración MVC
  mvc:
    static-path-pattern: /**

server:
  port: 8443
  ssl:
    key-store: file:/opt/deposito/keystore.p12
    key-store-password: gamasonic123
    key-store-type: PKCS12
    key-alias: tomcat
  error:
    include-stacktrace: always
    include-message: always
    include-exception: true

logging:
  level:
    root: WARN
    org.hibernate.SQL: DEBUG
    com.deposito.gamasonic: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.type: TRACE
    org.springframework.transaction: DEBUG